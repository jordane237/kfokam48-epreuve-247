/**
 * Configuration d'environnement du frontend.
 * Aucun secret ici : seule l'URL de l'API est nécessaire.
 * Voir .env.example à la racine du frontend.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080'
};
